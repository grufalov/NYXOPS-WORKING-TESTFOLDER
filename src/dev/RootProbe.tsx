export default function RootProbe() {
  return (
    <div style={{background:'yellow', color:'black', padding:16, fontSize:18}}>
      ROOT PROBE ✅ — if you can see this, React tree renders.
    </div>
  );
}
